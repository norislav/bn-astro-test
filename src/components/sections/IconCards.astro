---
import IconCard from '../ui/IconCard.astro';

const { heading, description, services } = Astro.props.Content;
---

<section class="section bg-gray-100">
  <div class="container flex flex-col items-center">
    <h2 class="services-section__heading text-center mb-4">{heading}</h2>
    <p class="services-section__description text-center text-lg mb-12">
      {description}
    </p>

    <div class="services-section__grid grid md:grid-cols-2 lg:grid-cols-3 gap-8">
      {services.map((service) => (
        <IconCard 
          iconName={service.iconName} 
          title={service.title} 
          description={service.description} 
        />
      ))}
    </div>
  </div>
</section>
