---
// BlogCard.astro

interface Props {
  title: string;
  category: string;
  readTime: string;
  description: string;
  imageUrl: string;
  link: string;
}

const { title, category, readTime, description, imageUrl, link } = Astro.props as Props;
---

<div class="blog-card bg-white rounded-lg shadow-md overflow-hidden">
  <img src={imageUrl} alt={title} class="object-cover w-full h-48" />
  <div class="p-8">
    <div class="flex items-center gap-2 text-sm text-gray-500 mb-2">
      <span class="category">{category}</span>
      <span>•</span>
      <span>{readTime}</span>
    </div>
    <h3 class="text-2xl text-left font-semibold mb-2">{title}</h3>
    <p class="text-left text-gray-700 mb-4">
      {description}
    </p>
    <a href={link} class="text-primary-500 font-medium hover:underline flex items-center gap-1">
      READ MORE <span>→</span>
    </a>
  </div>
</div>
